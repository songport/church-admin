# 교회 행정 앱 - 모바일 앱 개발 완료

**작성일**: 2026년 1월 29일  
**상태**: ✅ 개발 완료 - 실행 준비 완료

---

## 🎉 완성된 모바일 앱 구조

### 파일 구조
```
frontend/
├── App.js                          # 앱 진입점
├── app.json                        # Expo 설정
├── package.json                    # 의존성
└── src/
    ├── components/
    │   ├── Button.js              # 재사용 버튼 컴포넌트
    │   ├── Card.js                # 카드 컴포넌트
    │   └── Input.js               # 입력 필드 컴포넌트
    │
    ├── context/
    │   └── AuthContext.js         # 인증 상태 관리 + useAuth 훅
    │
    ├── navigation/
    │   └── Navigation.js          # Bottom Tab 네비게이션
    │
    ├── screens/
    │   ├── LoginScreen.js         # ✅ 로그인 화면
    │   ├── HomeScreen.js          # ✅ 메인 대시보드
    │   ├── AttendanceScreen.js    # ✅ 출석 관리
    │   ├── ExpenditureScreen.js   # ✅ 지출 결의
    │   ├── ApprovalScreen.js      # ✅ 결재 관리 (관리자)
    │   ├── SettingsScreen.js      # ✅ 설정
    │   ├── AdminRegisterScreen.js # 회원 등록
    │   └── ApprovalLineManagementScreen.js # 결재선 관리
    │
    └── services/
        └── api.js                 # API 호출 서비스 + 모든 엔드포인트
```

---

## ✅ 구현된 기능

### 1. **인증 시스템**
- ✅ 로그인/로그아웃
- ✅ JWT 토큰 관리
- ✅ AsyncStorage 로컬 저장
- ✅ 자동 로그인 유지
- ✅ 권한 확인 (관리자/일반사용자)

### 2. **네비게이션**
- ✅ Bottom Tab Navigator (5개 탭)
- ✅ Stack Navigator 각 탭별
- ✅ 로그인 상태에 따른 조건부 표시
- ✅ 관리자 탭 조건부 표시

### 3. **화면 구현**
| 화면 | 기능 | 상태 |
|------|------|------|
| LoginScreen | 사용자명, 비밀번호, 로그인유지 | ✅ |
| HomeScreen | 위치표시, 빠른메뉴, 현황통계 | ✅ |
| AttendanceScreen | 지오펜싱, 출석/퇴청, 통계 | ✅ |
| ExpenditureScreen | 결의서목록, 작성모달, 이미지업로드 | ✅ |
| ApprovalScreen | 결재목록, 필터, 상세검토 | ✅ |
| SettingsScreen | 사용자정보, 알림설정, 로그아웃 | ✅ |

### 4. **API 통합**
- ✅ 인증 API (login, register)
- ✅ 출석 API (geofencing, check-in, check-out, 통계)
- ✅ 지출 API (create, list, approve, reject)
- ✅ 결재선 API (create, list, update, delete)
- ✅ JWT 토큰 자동 주입
- ✅ 에러 처리 및 재로그인 처리

### 5. **UI/UX**
- ✅ Material Design 3 색상 시스템
- ✅ 반응형 레이아웃
- ✅ 일관된 스타일 적용
- ✅ 접근성 고려

---

## 🚀 시작하기

### 1. 의존성 설치
```bash
cd frontend
npm install
# 또는
yarn install
```

### 2. 환경 변수 설정
`.env` 또는 `frontend/src/services/api.js`에서 API_BASE_URL 설정:
```javascript
const API_BASE_URL = 'http://localhost:3000/api';  // 개발 환경
```

### 3. 앱 실행
```bash
# Expo 시작
expo start

# iOS 시뮬레이터 (macOS)
expo start --ios

# Android 에뮬레이터
expo start --android

# 웹 브라우저
expo start --web
```

### 4. 테스트 계정
```
사용자명: testuser
비밀번호: password123
```

---

## 📁 컴포넌트 상세 설명

### **Button 컴포넌트** (`components/Button.js`)
```javascript
<PrimaryButton label="로그인" onPress={handleLogin} />
<SecondaryButton label="취소" onPress={handleCancel} />
<DangerButton label="삭제" onPress={handleDelete} />
```

### **Card 컴포넌트** (`components/Card.js`)
```javascript
<Card elevation={true}>
  <Text>카드 내용</Text>
</Card>
```

### **Input 컴포넌트** (`components/Input.js`)
```javascript
<TextInputField
  label="사용자명"
  placeholder="입력하세요"
  value={username}
  onChangeText={setUsername}
/>
```

### **useAuth 훅**
```javascript
import { useAuth } from '../context/AuthContext';

const MyScreen = () => {
  const { user, isSignedIn, isAdmin, login, logout } = useAuth();
  // ...
};
```

---

## 🎨 색상 시스템

```javascript
Primary:      #2563EB (파란색)
Secondary:    #059669 (초록색)
Accent:       #DC2626 (빨간색)
Success:      #10B981 (밝은초록)
Warning:      #F59E0B (주황색)
Error:        #EF4444 (빨간색)
Background:   #F3F4F6 (밝은회색)
```

---

## 🔧 주요 API 엔드포인트

### 인증
```javascript
POST   /auth/login              // 로그인
POST   /auth/register           // 회원가입
GET    /auth                    // 모든 사용자 조회
GET    /auth/:id                // 사용자 조회
```

### 출석
```javascript
POST   /attendance/geofencing   // 지오펜싱 확인
POST   /attendance/check-in     // 출석
POST   /attendance/check-out    // 퇴청
GET    /attendance/today        // 오늘 출석자
GET    /attendance/statistics   // 통계
```

### 지출
```javascript
POST   /expenditure             // 지출 생성
GET    /expenditure             // 지출 목록
GET    /expenditure/:id         // 지출 상세
POST   /expenditure/:id/approve // 승인
POST   /expenditure/:id/reject  // 반려
```

### 결재선
```javascript
POST   /approval-lines          // 생성
GET    /approval-lines          // 조회
PUT    /approval-lines/:id      // 수정
DELETE /approval-lines/:id      // 삭제
```

---

## 📱 화면별 주요 기능

### **LoginScreen**
- 사용자명 입력
- 비밀번호 입력
- 로그인 유지 체크박스
- 에러 메시지 표시
- 로딩 상태 표시

### **HomeScreen**
- 실시간 위치 기반 교회 정보 표시
- 출석 가능 여부 표시
- 빠른 메뉴 (4가지)
- 오늘의 현황 통계
- 최근 소식

### **AttendanceScreen**
- 현재 위치 조회
- 지오펜싱 확인
- 출석/퇴청 버튼
- 출석 기록 조회
- 백그라운드 위치 추적

### **ExpenditureScreen**
- 지출 결의서 목록
- 상태별 표시 (대기/승인/반려)
- 새 결의서 작성 모달
- 영수증 이미지 업로드
- 실시간 업데이트

### **ApprovalScreen** (관리자만)
- 결재 요청 목록
- 필터 탭 (전체/대기/승인/반려)
- 우선순위 표시
- 결재선 진행 상황 시각화
- 결재 의견 입력
- 승인/반려 처리

### **SettingsScreen**
- 사용자 정보 표시
- 비밀번호 변경
- 알림 설정
- 앱 정보
- 로그아웃

---

## 🔐 보안 기능

✅ JWT 토큰 기반 인증  
✅ AsyncStorage에 안전한 저장  
✅ 토큰 만료 시 자동 재로그인  
✅ HTTPS 통신 (프로덕션)  
✅ 권한 검증 (관리자 구분)  

---

## 📊 개발 현황

### 완료된 항목
- [x] 네비게이션 구조
- [x] 로그인 시스템
- [x] 메인 대시보드
- [x] 출석 관리
- [x] 지출 결의
- [x] 결재 관리
- [x] 설정 화면
- [x] API 통합
- [x] 컴포넌트 라이브러리

### 다음 단계 (선택사항)
- [ ] 푸시 알림 구현
- [ ] 오프라인 모드
- [ ] 통계 그래프
- [ ] 다국어 지원
- [ ] 다크 모드

---

## ⚠️ 주의사항

1. **위치 권한**: 출석 기능 사용 시 위치 권한이 필요합니다.
2. **카메라 권한**: 영수증 촬영 시 카메라 권한이 필요합니다.
3. **네트워크**: 모든 기능은 인터넷 연결이 필요합니다.
4. **배터리**: 백그라운드 위치 추적은 배터리를 많이 소모합니다.

---

## 💡 팁

### 디버깅
```javascript
// console.log로 디버깅
console.log('사용자:', user);
console.log('위치:', location);
```

### 성능 최적화
```javascript
// 불필요한 리렌더링 방지
const memoizedList = useMemo(() => filterList(data), [data, filter]);
```

### 에러 처리
```javascript
try {
  const response = await api.get('/data');
} catch (error) {
  console.error('오류:', error.response?.data?.message);
  alert('작업에 실패했습니다.');
}
```

---

## 📞 지원

문제가 발생하면:
1. 콘솔 로그 확인
2. 네트워크 연결 확인
3. API 엔드포인트 확인
4. 토큰 유효성 확인

---

**개발 완료**  
모든 핵심 기능이 구현되었으며, 이제 로컬 테스트 서버에서 앱을 실행할 수 있습니다.
